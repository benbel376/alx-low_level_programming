#include "holberton.h"
/**
 * *_memset(char *s, char b, unsigned int n): fills memory with constant byte
 * return: returns a pointer.
 */

char *_memset(char *s, char b, unsigned int n)
{
int c;
for (c = 0; c <= n; c++)
{
s = b;
*s++;
}
*s -= n;
return (*s);
}
